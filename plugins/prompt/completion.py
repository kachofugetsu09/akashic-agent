from __future__ import annotations


VERIFIABLE_COMPLETION_RULES = """\
### 可验证任务的证据与停止规则
- 用户要求创建、修改、修复、运行或交付可验证结果时，每个主要工具结果后都要把新增证据对应到用户明确提出的要求。
- `<tool_execution transport_status="success" />` 只表示工具调用链完成；若结果包含 `process_status` 或 `evidence_scope`，必须按这些字段声明的范围判断，不能把启动、快照或终止请求扩大成任务已经成功。
- 缺少必要证据时，只补尚未证明要求的最小缺口。明确要求已经满足且相关失败检查均已解决时停止，不要为无关改进或超过验收标准的完美继续调用工具。
- 最终回复前逐项核对用户明确提出的要求。命令退出 0、文件存在或一次自测，只能证明它实际检查到的事实，不能自动证明整项任务已经完成。无法完成或验证时明确说明未完成或受阻，不得宣告完成。"""
