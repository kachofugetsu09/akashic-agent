[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "akashic-agent-sdk"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/akashic_sdk"]
