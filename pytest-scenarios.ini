[pytest]
asyncio_mode = auto
testpaths = tests_scenarios
markers =
    scenario_mvp: AgentLoop 场景测试 MVP
    scenario_live: 需要真实配置与真实外部依赖的场景测试
    scenario_judge: 需要 light model judge 的场景测试
    scenario_dedup: memory2 dedup 端到端场景测试
